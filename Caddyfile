(common) {
  header /* {
    -Server
  }
}

:{$PORT} {
  import common

  root * /usr/share/caddy
  encode zstd gzip

  route {
    handle /api/* {
      reverse_proxy phpapi:8080
    }

    try_files {path} {path}.html {path}/ /index.html
    file_server
  }
}
