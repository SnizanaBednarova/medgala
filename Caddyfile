(common) {
  header /* {
    -Server
  }
}

:{$PORT} {
  import common

  root * /usr/share/caddy
  encode zstd gzip

  handle_path /api/* {
    reverse_proxy php.railway.internal:8080
  }

  try_files {path} {path}.html {path}/ /index.html
  file_server
}
